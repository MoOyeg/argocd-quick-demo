---
apiVersion: batch/v1
kind: Job
metadata:
  name: vault-seed
  namespace: vault-demo
spec:
  template:
    spec:
      containers:
        - name: seed
          securityContext:
            runAsUser: 100
            runAsGroup: 1000
