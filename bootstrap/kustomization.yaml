apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Applied once, by hand:  kustomize build bootstrap | oc apply -f -
resources:
  - appproject-platform.yaml
  - appproject-apps.yaml
  - root-application.yaml
